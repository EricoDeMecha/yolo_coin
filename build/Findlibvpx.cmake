

function(conan_message MESSAGE_OUTPUT)
    if(NOT CONAN_CMAKE_SILENT_OUTPUT)
        message(${ARGV${0}})
    endif()
endfunction()


macro(conan_find_apple_frameworks FRAMEWORKS_FOUND FRAMEWORKS FRAMEWORKS_DIRS)
    if(APPLE)
        foreach(_FRAMEWORK ${FRAMEWORKS})
            # https://cmake.org/pipermail/cmake-developers/2017-August/030199.html
            find_library(CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND NAME ${_FRAMEWORK} PATHS ${FRAMEWORKS_DIRS} CMAKE_FIND_ROOT_PATH_BOTH)
            if(CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND)
                list(APPEND ${FRAMEWORKS_FOUND} ${CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND})
            else()
                message(FATAL_ERROR "Framework library ${_FRAMEWORK} not found in paths: ${FRAMEWORKS_DIRS}")
            endif()
        endforeach()
    endif()
endmacro()


function(conan_package_library_targets libraries package_libdir deps out_libraries out_libraries_target build_type package_name)
    unset(_CONAN_ACTUAL_TARGETS CACHE)
    unset(_CONAN_FOUND_SYSTEM_LIBS CACHE)
    foreach(_LIBRARY_NAME ${libraries})
        find_library(CONAN_FOUND_LIBRARY NAME ${_LIBRARY_NAME} PATHS ${package_libdir}
                     NO_DEFAULT_PATH NO_CMAKE_FIND_ROOT_PATH)
        if(CONAN_FOUND_LIBRARY)
            conan_message(STATUS "Library ${_LIBRARY_NAME} found ${CONAN_FOUND_LIBRARY}")
            list(APPEND _out_libraries ${CONAN_FOUND_LIBRARY})
            if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
                # Create a micro-target for each lib/a found
                string(REGEX REPLACE "[^A-Za-z0-9.+_-]" "_" _LIBRARY_NAME ${_LIBRARY_NAME})
                set(_LIB_NAME CONAN_LIB::${package_name}_${_LIBRARY_NAME}${build_type})
                if(NOT TARGET ${_LIB_NAME})
                    # Create a micro-target for each lib/a found
                    add_library(${_LIB_NAME} UNKNOWN IMPORTED)
                    set_target_properties(${_LIB_NAME} PROPERTIES IMPORTED_LOCATION ${CONAN_FOUND_LIBRARY})
                    set(_CONAN_ACTUAL_TARGETS ${_CONAN_ACTUAL_TARGETS} ${_LIB_NAME})
                else()
                    conan_message(STATUS "Skipping already existing target: ${_LIB_NAME}")
                endif()
                list(APPEND _out_libraries_target ${_LIB_NAME})
            endif()
            conan_message(STATUS "Found: ${CONAN_FOUND_LIBRARY}")
        else()
            conan_message(STATUS "Library ${_LIBRARY_NAME} not found in package, might be system one")
            list(APPEND _out_libraries_target ${_LIBRARY_NAME})
            list(APPEND _out_libraries ${_LIBRARY_NAME})
            set(_CONAN_FOUND_SYSTEM_LIBS "${_CONAN_FOUND_SYSTEM_LIBS};${_LIBRARY_NAME}")
        endif()
        unset(CONAN_FOUND_LIBRARY CACHE)
    endforeach()

    if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
        # Add all dependencies to all targets
        string(REPLACE " " ";" deps_list "${deps}")
        foreach(_CONAN_ACTUAL_TARGET ${_CONAN_ACTUAL_TARGETS})
            set_property(TARGET ${_CONAN_ACTUAL_TARGET} PROPERTY INTERFACE_LINK_LIBRARIES "${_CONAN_FOUND_SYSTEM_LIBS};${deps_list}")
        endforeach()
    endif()

    set(${out_libraries} ${_out_libraries} PARENT_SCOPE)
    set(${out_libraries_target} ${_out_libraries_target} PARENT_SCOPE)
endfunction()


include(FindPackageHandleStandardArgs)

conan_message(STATUS "Conan: Using autogenerated Findlibvpx.cmake")
# Global approach
set(libvpx_FOUND 1)
set(libvpx_VERSION "1.11.0")

find_package_handle_standard_args(libvpx REQUIRED_VARS
                                  libvpx_VERSION VERSION_VAR libvpx_VERSION)
mark_as_advanced(libvpx_FOUND libvpx_VERSION)


set(libvpx_INCLUDE_DIRS "/home/erico/.conan/data/libvpx/1.11.0/_/_/package/21d176c6d550e07ab7b17c1513a8ddb7fb960145/include")
set(libvpx_INCLUDE_DIR "/home/erico/.conan/data/libvpx/1.11.0/_/_/package/21d176c6d550e07ab7b17c1513a8ddb7fb960145/include")
set(libvpx_INCLUDES "/home/erico/.conan/data/libvpx/1.11.0/_/_/package/21d176c6d550e07ab7b17c1513a8ddb7fb960145/include")
set(libvpx_RES_DIRS )
set(libvpx_DEFINITIONS )
set(libvpx_LINKER_FLAGS_LIST
        "$<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,SHARED_LIBRARY>:>"
        "$<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,MODULE_LIBRARY>:>"
        "$<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,EXECUTABLE>:>"
)
set(libvpx_COMPILE_DEFINITIONS )
set(libvpx_COMPILE_OPTIONS_LIST "" "")
set(libvpx_COMPILE_OPTIONS_C "")
set(libvpx_COMPILE_OPTIONS_CXX "")
set(libvpx_LIBRARIES_TARGETS "") # Will be filled later, if CMake 3
set(libvpx_LIBRARIES "") # Will be filled later
set(libvpx_LIBS "") # Same as libvpx_LIBRARIES
set(libvpx_SYSTEM_LIBS stdc++)
set(libvpx_FRAMEWORK_DIRS )
set(libvpx_FRAMEWORKS )
set(libvpx_FRAMEWORKS_FOUND "") # Will be filled later
set(libvpx_BUILD_MODULES_PATHS )

conan_find_apple_frameworks(libvpx_FRAMEWORKS_FOUND "${libvpx_FRAMEWORKS}" "${libvpx_FRAMEWORK_DIRS}")

mark_as_advanced(libvpx_INCLUDE_DIRS
                 libvpx_INCLUDE_DIR
                 libvpx_INCLUDES
                 libvpx_DEFINITIONS
                 libvpx_LINKER_FLAGS_LIST
                 libvpx_COMPILE_DEFINITIONS
                 libvpx_COMPILE_OPTIONS_LIST
                 libvpx_LIBRARIES
                 libvpx_LIBS
                 libvpx_LIBRARIES_TARGETS)

# Find the real .lib/.a and add them to libvpx_LIBS and libvpx_LIBRARY_LIST
set(libvpx_LIBRARY_LIST vpx)
set(libvpx_LIB_DIRS "/home/erico/.conan/data/libvpx/1.11.0/_/_/package/21d176c6d550e07ab7b17c1513a8ddb7fb960145/lib")

# Gather all the libraries that should be linked to the targets (do not touch existing variables):
set(_libvpx_DEPENDENCIES "${libvpx_FRAMEWORKS_FOUND} ${libvpx_SYSTEM_LIBS} ")

conan_package_library_targets("${libvpx_LIBRARY_LIST}"  # libraries
                              "${libvpx_LIB_DIRS}"      # package_libdir
                              "${_libvpx_DEPENDENCIES}"  # deps
                              libvpx_LIBRARIES            # out_libraries
                              libvpx_LIBRARIES_TARGETS    # out_libraries_targets
                              ""                          # build_type
                              "libvpx")                                      # package_name

set(libvpx_LIBS ${libvpx_LIBRARIES})

foreach(_FRAMEWORK ${libvpx_FRAMEWORKS_FOUND})
    list(APPEND libvpx_LIBRARIES_TARGETS ${_FRAMEWORK})
    list(APPEND libvpx_LIBRARIES ${_FRAMEWORK})
endforeach()

foreach(_SYSTEM_LIB ${libvpx_SYSTEM_LIBS})
    list(APPEND libvpx_LIBRARIES_TARGETS ${_SYSTEM_LIB})
    list(APPEND libvpx_LIBRARIES ${_SYSTEM_LIB})
endforeach()

# We need to add our requirements too
set(libvpx_LIBRARIES_TARGETS "${libvpx_LIBRARIES_TARGETS};")
set(libvpx_LIBRARIES "${libvpx_LIBRARIES};")

set(CMAKE_MODULE_PATH "/home/erico/.conan/data/libvpx/1.11.0/_/_/package/21d176c6d550e07ab7b17c1513a8ddb7fb960145/" ${CMAKE_MODULE_PATH})
set(CMAKE_PREFIX_PATH "/home/erico/.conan/data/libvpx/1.11.0/_/_/package/21d176c6d550e07ab7b17c1513a8ddb7fb960145/" ${CMAKE_PREFIX_PATH})

if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
    # Target approach
    if(NOT TARGET libvpx::libvpx)
        add_library(libvpx::libvpx INTERFACE IMPORTED)
        if(libvpx_INCLUDE_DIRS)
            set_target_properties(libvpx::libvpx PROPERTIES INTERFACE_INCLUDE_DIRECTORIES
                                  "${libvpx_INCLUDE_DIRS}")
        endif()
        set_property(TARGET libvpx::libvpx PROPERTY INTERFACE_LINK_LIBRARIES
                     "${libvpx_LIBRARIES_TARGETS};${libvpx_LINKER_FLAGS_LIST}")
        set_property(TARGET libvpx::libvpx PROPERTY INTERFACE_COMPILE_DEFINITIONS
                     ${libvpx_COMPILE_DEFINITIONS})
        set_property(TARGET libvpx::libvpx PROPERTY INTERFACE_COMPILE_OPTIONS
                     "${libvpx_COMPILE_OPTIONS_LIST}")
        
    endif()
endif()

foreach(_BUILD_MODULE_PATH ${libvpx_BUILD_MODULES_PATHS})
    include(${_BUILD_MODULE_PATH})
endforeach()
